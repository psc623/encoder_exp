# Site-specific data sources. Copy to .env and source it; the application does
# not implicitly read .env files.
ADNI_SOURCE_MANIFEST=/absolute/path/to/brain_fm_manifest.csv
UCLA_BIDS_ROOT=/absolute/path/to/ds000030
UCLA_PARTICIPANTS_TSV=/absolute/path/to/ds000030/participants.tsv

# Select another YAML file if the fixed workspace is intentionally relocated.
ENCODERBENCH_CONFIG=/net/projects2/litian-lab/scpan/encoders/config/default.yaml
